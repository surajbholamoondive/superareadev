# About

`Please add some description`

## Scripts

- `build` preconfigured next build except that linter has been excluded from it
  but will be reverted back.
- `lint` execute linter across the repo
- `lfix` fixes fixable linting errors reported by eslint

### Caveats

Please run `npm install` before running any commands from scripts section as the
configuration has been changed.
